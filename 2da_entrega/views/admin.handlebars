<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Administrador</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #e9ecef; /* Color de fondo más suave */
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: #333; /* Color del texto */
        }

        .navbar {
            width: 100%;
            background-color: #007bff; /* Color de la barra de navegación */
            padding: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            position: fixed;
            top: 0;
            left: 0;
            z-index: 1000;
        }

        .navbar-brand {
            font-size: 24px;navegación */
            text-decoration: none;
        }

        h1 {
            margin-top: 70px; /* Asegúrate de que el título no se superponga a la barra de navegación */
            color: #343a40; /* Color del título */
        }

        .container {
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            width: 300px;
            margin: 20px 0;
        }

        h2 {
            text-align: center;
        }

        label {
            margin: 5px 0;
            display: block;
            color: #555;
        }

        input[type="text"],
        input[type="password"],
        input[type="email"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 3px;
            box-sizing: border-box; /* Asegura que el padding y el border estén incluidos en el ancho */
        }

        button,
        input[type="submit"] {
            width: 100%;
            padding: 15px; /* Aumentar el tamaño del botón */
            border: none;
            border-radius: 3px;
            color: white;
            font-size: 18px; /* Aumentar el tamaño del texto */
            cursor: pointer;
            background-color: #28a745; /* Color del botón de inicio */
            transition: background-color 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #218838; /* Color del botón al pasar el mouse */
        }

        .logout-link {
            text-align: center;
            margin-top: 15px;
        }

        .logout-link a {
            color: #007bff; /* Color del enlace de salir */
            text-decoration: none;
        }

        .logout-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Precoce-Italiano</a>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0"></ul>
            </div>
        </div>
    </nav>

    <h1>Administrador</h1>

    <div class="container" id="create-account-section">
        <h2>Iniciar Sesión</h2>
        <form action="/main_ad" method="post">
            <label for="username">Nombre de Usuario:</label>
            <input type="text" id="username" name="username" required><br><br>

            <label for="password">Contraseña:</label>
            <input type="password" id="password" name="password" required><br><br>

            <input type="submit" value="Iniciar" class="login-account">

            <div>
            <a href="/register" >Salir del Administrador</a> 
            </div>
        </form>
    </div>
    
</body>
</html>
